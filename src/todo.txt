
Idee:
	ZeroMq wegabstrahieren und type safe machen
	eine Instanz zur Verwendung, die den gewünschten Socket bei Bedarf baut.

    Logging für jeden Step

core
	later:
		async / Router dealer socket
		Discovery
        Pub / Sub

Backlog:
features
    Logging (LibLog)
    XtResult erweitern/verbessern mit callbacks
    injection (SimpleInjector)
    socket als bus ? (eine Instanz in der App)
    Demistify for stacktrace
    Retries auf request ebene
    Performance (Utf8)

    CleanUp (also in tests)

sonstiges
    automatisierte tests
    github hochladen
    last test
